# 基于多兴趣序列建模与内容融合的动漫追番推荐系统设计与实现

## 一、项目整体定位
本项目以动漫追番场景为核心落地载体，针对传统协同过滤忽略时序信息、纯序列推荐存在冷启动短板、单兴趣建模无法覆盖用户多元追番需求三大痛点，构建「**序列行为建模 + 多兴趣拆分 + 内容语义融合 + 可解释性输出**」的四层推荐算法架构，并配套完整的前后端交互系统与可视化分析模块，形成从算法实验到工程落地的完整闭环。

### 核心创新点
1. 算法层面：在经典SASRec基础上引入多兴趣胶囊网络，解决用户多题材追番的兴趣拆分问题；融合内容语义特征缓解新番冷启动
2. 系统层面：覆盖追番全生命周期管理，新增兴趣漂移可视化分析、新番专属推荐两大场景化功能
3. 实验层面：设计多维度对比实验，从整体效果、冷启动、分题材覆盖、消融验证四个维度全面验证模型有效性

---

## 二、全栈技术栈选型

### 1. 算法与模型层
| 模块 | 技术选型 | 选型说明 |
|------|----------|----------|
| 深度学习框架 | PyTorch 2.x | 生态完善，SASRec及变体开源实现丰富，调试便捷 |
| 基础序列模型 | SASRec | 序列推荐经典基准，因果自注意力适配追番时序场景 |
| 多兴趣建模 | 动态路由胶囊网络（参考MIND） | 轻量易实现，能有效拆分用户多元兴趣，计算开销低 |
| 内容语义编码 | DistilBERT | 参数量小、推理快，适配动漫剧情简介与标签的语义向量化 |
| 数据处理 | Pandas + NumPy + Scikit-learn | 数据集清洗、序列构造、指标计算 |
| 实验可视化 | Matplotlib + Seaborn | 绘制指标对比曲线、消融实验结果图 |

### 2. 后端服务层
- **Web框架**：FastAPI
  轻量高性能，自动生成接口文档，模型服务化部署简单快速，适合毕设快速迭代
- **服务形态**：RESTful API
  统一封装推荐计算、用户管理、追番管理等核心接口
- **异步任务**：APScheduler
  定时离线计算全量用户推荐结果、每日更新新番推荐池

### 3. 前端交互层
- **核心框架**：Vue 3 + Vite
- **UI组件库**：Element Plus
  组件丰富，后台管理系统和用户页面开发效率高
- **可视化图表**：ECharts
  实现兴趣漂移雷达图、观看时间轴、推荐效果统计等可视化

### 4. 数据存储层
- **关系型数据库**：MySQL 8.0
  存储用户信息、动漫元数据、追番记录、系统配置等结构化数据
- **缓存数据库**：Redis
  缓存热门用户推荐结果、新番推荐列表，缩短接口响应时间
- **模型文件**：本地文件存储
  保存训练好的模型权重、物品嵌入矩阵、内容语义向量
### 5. 版本控制
- **分布式协同版本控制**：git--传至github
---

## 三、核心算法设计（四大模块）

### 1. 基础层：SASRec 因果序列推荐基座
作为整个系统的算法底座，负责从用户追番序列中提取行为兴趣。
- **输入**：用户按时间排序的追番序列 `[v₁, v₂, ..., vₙ]`
- **核心结构**：物品嵌入 + 可学习位置嵌入 → 2层因果掩码多头自注意力 → 前馈网络 + 残差归一化
- **输出**：用户序列的最终行为表示向量
- **训练策略**：滑动窗口扩充训练样本 + 负采样 + 二元交叉熵损失 + 早停机制

### 2. 改进层1：多兴趣胶囊网络建模
解决单兴趣向量淹没用户小众追番偏好的问题。
- **接入位置**：SASRec最后一层自注意力输出之后
- **实现逻辑**：
  1. 将序列中每个位置的特征向量作为初级胶囊
  2. 通过动态路由算法，聚合生成 `K=4` 个高级兴趣胶囊（对应4类不同的追番兴趣）
  3. 每个胶囊代表用户一个独立的兴趣维度（如热血战斗、日常治愈、恋爱校园、悬疑推理）
- **预测方式**：每个兴趣胶囊分别召回候选动漫，合并去重后统一排序输出Top-N

### 3. 改进层2：内容语义特征融合
解决新番无交互数据的冷启动问题，同时提升推荐的题材匹配度。
- **内容特征提取**：
  - 输入：动漫的题材标签、剧情简介文本
  - 输出：512维内容语义向量（DistilBERT编码）
- **双路融合机制**：
  1. **候选侧融合**：将内容向量与物品嵌入向量拼接，作为最终的物品表示，从根源上让新番具备初始特征
  2. **排序侧融合**：最终排序时，行为序列得分与内容相似度得分按7:3加权求和
- **冷启动适配**：对于交互数<10的新番，提高内容相似度的权重至50%

### 4. 增强层：可解释性输出
将黑盒模型转化为可解释的推荐理由，提升答辩展示效果。
- **行为层面解释**：提取注意力权重最高的Top3历史番剧，标注“核心影响番剧”
- **内容层面解释**：计算推荐番与用户历史番的题材标签重合度，输出“题材匹配度XX%”
- **结构化输出**：前端展示格式示例：
  > 推荐《咒术回战》
  > 匹配原因：你近期观看了《进击的巨人》《鬼灭之刃》等热血战斗番（题材匹配度87%）

---

## 四、完整系统功能架构

### 1. 用户端核心模块
#### （1）账号与追番管理
- 用户注册登录、个人信息设置
- 追番状态管理：标记「想看/在看/已看/弃番」，支持评分、添加观看时间
- 历史观看记录时间轴展示

#### （2）个性化推荐中心
- 综合推荐页：基于用户全量追番序列的Top20推荐
- 分类推荐页：按题材分类的细分推荐
- 可解释性展示：每条推荐附带匹配理由与核心影响番剧

#### （3）新番专属专区
- 按季度更新新番池，基于内容语义匹配用户兴趣
- 标注「冷启动推荐」标识，单独分区展示
- 支持用户反馈“感兴趣/不感兴趣”，迭代优化推荐

#### （4）兴趣漂移分析看板
- 兴趣雷达图：展示用户当前在各大题材上的兴趣分布
- 时间趋势图：按季度展示用户追番题材的变化曲线
- 漂移点标注：自动识别用户兴趣发生明显转变的时间节点

### 2. 管理后台模块
- 动漫数据管理：新增、编辑、下架动漫条目，维护标签与简介
- 用户管理：查看用户列表、用户追番数据统计
- 推荐效果监控：实时展示全平台HR、NDCG指标，用户点击率统计
- 冷启动数据看板：新番推荐的曝光、点击、收藏转化率

### 3. 推荐引擎服务
- **离线计算链路**：每日凌晨批量计算全量用户的推荐结果，写入Redis缓存
- **在线更新链路**：用户新增/修改追番记录后，异步更新用户序列，增量重排推荐结果
- **新番更新任务**：每周同步新番数据，生成内容向量，更新冷启动推荐池

---

## 五、项目开发全流程

### 阶段1：数据预处理与数据集构建（约占总工作量15%）
1. **原始数据清洗**
   - 处理MAL数据集的`anime.csv`与`rating.csv`，去除缺失值、异常值
   - 过滤交互数<5的冷启动用户、交互数<10的冷门番剧
2. **序列数据集构造**
   - 按用户分组，按时间戳升序排序，生成每个用户的完整追番序列
   - 采用「留一法」划分：倒数第1条为测试集，倒数第2条为验证集，其余为训练集
   - 滑动窗口扩充训练样本，设置最大序列长度`max_len=50`
3. **内容特征库构建**
   - 提取所有动漫的题材标签与剧情简介
   - 批量调用DistilBERT生成内容语义向量，保存为特征文件
4. **标签体系建设**
   - 统一动漫题材分类（热血、战斗、日常、治愈、恋爱、校园、悬疑、科幻等12类）
   - 构建新番子集：筛选发布时间晚、交互数少的番剧，作为冷启动测试集

### 阶段2：核心算法研发与对比实验（约占总工作量35%）
1. **基线模型复现**
   - 实现ItemCF协同过滤、GRU4Rec序列模型作为对比基线
   - 复现原版SASRec，调参至最优效果作为基准线
2. **改进模型实现**
   - 实现多兴趣胶囊网络模块，嵌入SASRec输出层
   - 实现内容特征融合模块，完成双路融合逻辑
   - 实现可解释性输出接口，输出注意力权重与匹配度
3. **多组实验验证**
   - 整体效果对比实验：4个模型在全量测试集上的HR@5/10、NDCG@5/10
   - 消融实验：分别移除多兴趣模块、内容融合模块，验证各模块的增益
   - 冷启动专项测试：在新番子集上单独测试指标提升
   - 分题材指标统计：按12类题材分别计算召回率，验证多兴趣对小众题材的覆盖提升
4. **实验结果分析与可视化**
   - 绘制指标对比柱状图、折线图
   - 输出实验结论，整理算法创新点

### 阶段3：全栈系统开发与联调（约占总工作量35%）
1. **数据库设计与搭建**
   - 设计用户表、动漫表、追番记录表、推荐结果表等核心表结构
   - 导入清洗后的动漫元数据，初始化基础数据
2. **后端接口开发**
   - 用户认证、追番管理、动漫查询等基础业务接口
   - 推荐接口：综合推荐、分类推荐、新番推荐、可解释性理由接口
   - 统计分析接口：兴趣分布数据、效果监控数据
   - 定时任务开发：离线推荐计算、新番更新
3. **前端页面开发**
   - 用户端：登录页、首页推荐、追番列表、新番专区、兴趣分析页
   - 管理端：数据管理、用户管理、效果监控看板
4. **前后端联调**
   - 接口联调与数据对齐
   - 模型服务接入，验证推荐结果正确性
   - 页面交互优化与bug修复

### 阶段4：测试（约占总工作量15%）
 **系统测试**
   - 功能测试：全流程走通，验证所有模块功能
   - 性能测试：推荐接口响应时间、并发访问稳定性
   - 效果测试：抽样验证推荐结果合理性与可解释性准确性


---

## 六、实验方案与评价体系

### 1. 基线对比实验
| 模型类型 | 具体模型 | 实验目的 |
|----------|----------|----------|
| 传统协同过滤 | Item-CF | 验证序列建模相对于传统方法的优势 |
| 经典序列模型 | GRU4Rec | 验证自注意力相对于循环神经网络的优势 |
| 基准序列模型 | 原版SASRec | 验证本项目改进点的增量增益 |
| 本文模型 | 多兴趣+内容融合SASRec | 验证最终方案的综合效果 |

### 2. 消融实验
| 实验组 | 配置 | 验证目标 |
|--------|------|----------|
| 实验组1 | 纯SASRec | 基准线 |
| 实验组2 | SASRec + 多兴趣 | 多兴趣模块的单独增益 |
| 实验组3 | SASRec + 内容融合 | 内容融合的单独增益 |
| 实验组4 | 完整模型（多兴趣+内容融合） | 两者结合的协同增益 |

### 3. 冷启动专项测试
- 测试集：筛选交互数<10的新番，对应测试用户样本
- 评价指标：HR@10、NDCG@10
- 对比维度：完整模型 vs 纯SASRec，验证内容融合对冷启动的缓解效果

### 4. 分题材细分指标
- 将所有动漫按12类题材划分
- 分别计算每类题材的Recall@10
- 重点观察小众题材（如悬疑、科幻）的召回率提升，验证多兴趣建模的价值

### 5. 评价指标计算
统一采用序列推荐标准评估方式：
- **HR@K**：命中用户占比，衡量推荐的准确率
- **NDCG@K**：考虑排序位置的归一化折损累计增益，衡量排序质量
- K取5、10两个常用档位，所有指标取全部测试用户的平均值
