# =============================================================
# 多智能体动漫追番推荐系统 —— Python 依赖
# 建议 Python 3.10 / 3.11（PyTorch 生态兼容性最好）
# 安装：pip install -r requirements.txt
# 版本区间为「经验可用区间」，锁死版本请用 pip freeze > requirements.lock
# =============================================================

# ---------- Web 服务 ----------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.6,<3.0
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27

# ---------- Agent 编排 / LLM ----------
langgraph>=0.1.5
langchain-core>=0.2
openai>=1.30          # 统一 OpenAI 兼容客户端（DashScope / DeepSeek / Ollama 均可用）

# ---------- 深度学习 ----------
torch>=2.1,<2.6
numpy>=1.24,<2.0
transformers>=4.40,<5.0
tokenizers>=0.19
safetensors>=0.4

# ---------- 数据处理与评估 ----------
pandas>=2.1,<3.0
scipy>=1.11
scikit-learn>=1.4
tqdm>=4.66

# ---------- 存储 ----------
pymysql>=1.1
SQLAlchemy>=2.0,<3.0
alembic>=1.13
redis>=5.0

# ---------- 任务调度 ----------
APScheduler>=3.10,<4.0

# ---------- 向量检索（内容召回）----------
faiss-cpu>=1.8        # 有 GPU 可换 faiss-gpu

# ---------- 认证安全 ----------
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# ---------- 实验可视化 ----------
matplotlib>=3.8
seaborn>=0.13

# ---------- 开发工具 ----------
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
black>=24.4
pre-commit>=3.7
